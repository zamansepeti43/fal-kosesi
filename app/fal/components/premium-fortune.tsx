"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import type { CSSProperties } from "react";
import { ArrowLeft, BriefcaseBusiness, Heart, Moon, Sparkles, WalletCards, Wand2 } from "lucide-react";
import type { FalKind, ReadingResult } from "@/lib/ai/provider";
import type { DigitalCommentator } from "@/lib/fortune/catalog";
import { DIGITAL_COMMENTATORS } from "@/lib/fortune/catalog";
import CommentatorPicker from "./commentator-picker";

type Props = { kind: FalKind; title: string; eyebrow: string; description: string; placeholder: string; accent?: "rose" | "emerald" | "violet" | "amber" | "blue" };
const accents = { rose: "from-rose-500 to-fuchsia-500", emerald: "from-emerald-500 to-lime-400", violet: "from-violet-500 to-fuchsia-500", amber: "from-amber-400 to-orange-400", blue: "from-sky-500 to-indigo-500" };
const fields = [["love", "Aşk", Heart], ["career", "İş & Kariyer", BriefcaseBusiness], ["money", "Para & Kısmet", WalletCards], ["future", "Yakın Gelecek", Moon]] as const;
const prompts: Partial<Record<FalKind, [string, string]>> = { love: ["Şu an aşk hayatındaki durumu kısaca anlatır mısın?", "Aşk konusunda özellikle hangi sorunun cevabını istiyorsun?"], money: ["Para ve kısmet tarafında şu an en önemli durum ne?", "Maddi konuda özellikle neyi öğrenmek istiyorsun?"], career: ["İş veya kariyerinde şu an hangi noktadasın?", "Kariyer konusunda özellikle hangi cevabı arıyorsun?"], future: ["Hayatında değişmesini veya netleşmesini beklediğin şey ne?", "Yakın gelecek için özellikle neyi merak ediyorsun?"], daily: ["Bugün seni en çok meşgul eden konu ne?", "Bugün için özellikle hangi konuda mesaj istiyorsun?"], dream: ["Rüyanda ne gördün ve en güçlü duygu neydi?", "Bu rüyanın özellikle hangi yönünü anlamak istiyorsun?"], astrology: ["Doğum tarihin, yerin ve biliyorsan saatin nedir?", "Haritanda özellikle hangi konuyu merak ediyorsun?"], numerology: ["Doğum tarihin ve kullanmak istediğin isim nedir?", "Sayılarının özellikle hangi konuda yol göstermesini istiyorsun?"], general: ["Şu an hayatında hangi konu öne çıkıyor?", "Falcıya soracağın asıl soru nedir?"] };
const SPRITE = "/fortune/avatars/non-tarot-characters.svg?v=1";
function spriteStyle(slot: number): CSSProperties { return { backgroundImage: `url(${SPRITE})`, backgroundSize: "500% 100%", backgroundPosition: `${slot * 25}% center` }; }

export default function PremiumFortune({ kind, title, eyebrow, description, placeholder, accent = "violet" }: Props) {
  const [step, setStep] = useState<0 | 1 | 2>(0), [answers, setAnswers] = useState(["", ""]), [reading, setReading] = useState<ReadingResult | null>(null), [loading, setLoading] = useState(false), [error, setError] = useState(""), [commentator, setCommentator] = useState<DigitalCommentator | null>(null);
  const gradient = accents[accent], qs = prompts[kind] ?? prompts.general!, fallback = useMemo(() => DIGITAL_COMMENTATORS.find((x) => x.specialties.includes(kind as never)), [kind]);
  const price = commentator?.priceCredits ?? fallback?.priceCredits ?? 8;
  const commentatorSlot = useMemo(() => { const list = DIGITAL_COMMENTATORS.filter((x) => x.specialties.includes(kind as never)); return Math.max(0, list.findIndex((x) => x.id === commentator?.id)); }, [commentator, kind]);
  const setAnswer = (i: number, v: string) => setAnswers((a) => a.map((x, n) => n === i ? v : x));
  const run = async () => { if (!commentator) return setError("Önce yorum yapacak sanal karakteri seç."); setLoading(true); setError(""); try { let profile: Record<string, unknown> = {}; try { profile = JSON.parse(localStorage.getItem("fal-kosesi-profile") || "{}"); } catch {} const question = [`Durum: ${answers[0].trim() || "Belirtilmedi"}`, `Özel soru: ${answers[1].trim() || "Belirtilmedi"}`].join("\n"); const r = await fetch("/api/fal/reading", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ kind, focus: kind, question, profile, commentatorId: commentator.id, deliveryMode: "instant" }) }); const data = await r.json(); if (!r.ok) throw new Error(data.error || "Yorum hazırlanamadı."); setReading(data); } catch (e) { setError(e instanceof Error ? e.message : "Bir hata oluştu."); } finally { setLoading(false); } };
  if (reading) return <main className="min-h-screen bg-[#070711] px-4 py-5 text-white"><div className="mx-auto max-w-3xl"><button onClick={() => setReading(null)} className="mb-5 text-xs text-slate-300">← Yeni yorum</button><section className={`rounded-[28px] bg-gradient-to-br ${gradient} p-6`}><p className="text-[9px] font-black uppercase tracking-[.25em]">{eyebrow}</p><h1 className="mt-2 font-serif text-3xl font-black">{title}</h1></section><section className="mt-4 rounded-[24px] border border-white/10 bg-white/[.035] p-5"><div className="flex gap-2 text-amber-200"><Sparkles /><h2 className="font-serif text-xl font-bold">Sana özel yorum</h2></div><p className="mt-4 text-sm leading-7 text-slate-200">{reading.summary}</p></section><section className="mt-5 grid gap-3 sm:grid-cols-2">{fields.filter(([k]) => reading.sections?.[k]).map(([k, label, Icon]) => <article key={k} className="rounded-[22px] border border-white/10 bg-white/[.035] p-5"><div className="flex gap-3"><Icon className="h-5 w-5 text-violet-200" /><h3 className="font-serif text-lg font-bold">{label}</h3></div><p className="mt-4 text-sm leading-7 text-slate-200">{reading.sections[k]}</p></article>)}</section><Link href="/kredi" className="mt-5 inline-flex rounded-full bg-amber-300 px-5 py-3 text-sm font-black text-slate-950">Kredi yükle</Link></div></main>;
  return <main className="min-h-screen bg-[radial-gradient(circle_at_top,rgba(244,63,94,.18),transparent_35%),linear-gradient(180deg,#080817,#05050d)] px-4 pb-12 pt-4 text-white"><div className="mx-auto max-w-2xl"><header className="mb-5 flex items-center justify-between"><button onClick={() => step === 0 ? window.history.back() : setStep((s) => Math.max(0, s - 1) as 0 | 1 | 2)} className="flex items-center gap-1 text-xs text-slate-300"><ArrowLeft className="h-4 w-4" />Geri</button><span className="rounded-full border border-violet-300/15 bg-violet-300/5 px-3 py-1 text-[9px] font-bold uppercase tracking-[.2em] text-violet-200">Fal Köşesi</span></header><section className="overflow-hidden rounded-[30px] border border-white/10 bg-[#0d0b18]/95 shadow-2xl"><div className="px-5 pb-6 pt-8 text-center"><div className="mx-auto grid h-24 w-24 place-items-center rounded-full border border-rose-300/25 bg-gradient-to-br from-rose-500/20 to-violet-500/20"><Wand2 className="h-10 w-10 text-rose-200" /></div><p className="mt-5 text-[9px] font-black uppercase tracking-[.3em] text-amber-200">{eyebrow}</p><h1 className="mt-1 font-serif text-3xl font-black">{title}</h1><p className="mx-auto mt-2 max-w-lg text-xs leading-6 text-slate-400">{description}</p></div><div className="border-t border-white/5 px-5 py-5 sm:px-8"><div className="mb-6 flex justify-center gap-2">{[0,1,2].map((i) => <span key={i} className={`h-2 w-10 rounded-full ${i <= step ? "bg-rose-400" : "bg-white/10"}`} />)}</div>{step === 0 && <div className="space-y-4"><p className="text-[9px] font-bold uppercase tracking-[.25em] text-rose-200">1 · Yorumcunu seç</p><h2 className="font-serif text-2xl font-bold">Bu falı kim yorumlasın?</h2><CommentatorPicker kind={kind} selectedId={commentator?.id ?? null} onSelect={setCommentator} />{error && <p className="rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-xs text-red-200">{error}</p>}<button onClick={() => setStep(1)} disabled={!commentator} className="w-full rounded-full bg-gradient-to-r from-rose-500 to-violet-500 px-6 py-4 text-sm font-black disabled:opacity-50">Devam Et</button></div>}{step === 1 && <div className="space-y-4"><p className="text-[9px] font-bold uppercase tracking-[.25em] text-rose-200">2 · Kısa kişisel bağlam</p>{qs.map((label, i) => <label key={label} className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-200">{i + 1}. {label}</span><textarea value={answers[i]} onChange={(e) => setAnswer(i, e.target.value)} rows={i === 1 ? 4 : 3} placeholder={i === 1 ? placeholder : "Kısaca anlatabilirsin…"} className="w-full resize-none rounded-2xl border border-white/10 bg-black/20 px-4 py-3 text-sm text-white outline-none focus:border-rose-400" /></label>)}<button onClick={() => setStep(2)} className="w-full rounded-full bg-gradient-to-r from-rose-500 to-violet-500 px-6 py-4 text-sm font-black">Devam Et</button></div>}{step === 2 && <div className="space-y-4"><p className="text-center text-[9px] font-bold uppercase tracking-[.25em] text-amber-200">3 · Son adım</p><h2 className="text-center font-serif text-2xl font-bold">Falını başlat</h2><div className="flex items-center gap-3 rounded-[24px] border border-rose-300/15 bg-rose-300/[.04] p-4"><div role="img" aria-label="Seçilen sanal karakter" className="h-16 w-16 shrink-0 rounded-2xl bg-center bg-no-repeat" style={spriteStyle(commentatorSlot)} /><div className="min-w-0"><p className="font-serif text-lg font-bold">{commentator?.name ?? fallback?.name ?? "Sanal Yorumcu"}</p><p className="text-xs text-violet-200">{commentator?.title ?? fallback?.title ?? "Sanal Yorumcu"}</p></div><strong className="ml-auto text-amber-200">{price} kredi</strong></div>{error && <p className="rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-xs text-red-200">{error}</p>}<button disabled={loading} onClick={run} className="w-full rounded-full bg-gradient-to-r from-rose-500 to-violet-500 px-6 py-4 text-sm font-black disabled:opacity-60">{loading ? "Falın hazırlanıyor…" : `${price} krediyle falımı başlat`}</button><p className="text-center text-[9px] text-slate-600">Eğlence ve kişisel farkındalık amaçlı sembolik yorum.</p></div>}</div></section></div></main>;
}
